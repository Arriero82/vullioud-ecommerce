import ItemListContainer from "./ItemListContainer";

const Main = (props) =>{
    return(
        <main>
            <ItemListContainer greeting="Tickets for next concerts!"/>
        </main>
    )
};

export default Main;